# SyncGen

A poerful Streamlit app that uses LLM to generate image via prompts.
This app allows users to enter a prompt, enchance the user prompt given, pass the prompt to an LLM for the Generation to happen.
We will be using Bria AI's API for generating the image since we don't have the compute power.

## Features
1. Generate UHQ images from text prompts
2. Remove background.
3. Prompt Enhancements.
4. Add realistic shadows

# Setup
## Create an environment varaible
- Create .env file
- We need these URLS and an API key from Bria Ai.

1. BRIA_API_KEY = `"your_api_key"`
2. ENHANCE_PROMPT_URL = `"url-1"`  # This is responsible for the prompt enhancement.
3. HD_IMAGE_URL = `"url-2"` # This URL is responsible for generating HD images.

# Coming Soon...
1. Fast image generations.
2. Vector Image generations
3. Image Editing (image-to-image)
4. Tailored Generations.

In all;
1. We will make huge upgrades to the existing codebase
2. Add a Streamlit front-end.
3. Create an API for this project.

### This is going to be FUN.....

`#open-source 😂😂😂😂😂😂😂😂`